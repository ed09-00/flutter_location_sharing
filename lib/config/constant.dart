
import 'package:geolocator/geolocator.dart';

LocationSettings locationSettings = const LocationSettings(
    accuracy: LocationAccuracy.high,
    distanceFilter: 5,
  );

double connectPageButtonFontSize = 30;